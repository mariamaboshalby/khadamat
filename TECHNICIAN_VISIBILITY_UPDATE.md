# تحديث: إخفاء الطلبات عن الفنيين الآخرين بعد التقديم

## التاريخ: 2025
## الهدف: جعل الطلب يظهر فقط للفني الذي قدم عليه، ولا يظهر لباقي الفنيين

---

## التغييرات المطبقة

### 1. تعديل دالة `index()` في RequestController

**قبل التعديل:**
```php
// كان يعرض الطلبات بحالة approved و pricing_pending
$query = RequestModel::with(['user', 'service', 'service.specialization'])
    ->whereIn('status', ['approved', 'pricing_pending'])
    ->whereHas('service', function($q) use ($technician) {
        $q->where('specialization_id', $technician->specialization_id);
    });
```

**بعد التعديل:**
```php
// الآن يعرض فقط الطلبات بحالة approved وغير معينة لأي فني
$query = RequestModel::with(['user', 'service', 'service.specialization'])
    ->where('status', 'approved')
    ->whereNull('assigned_technician_id')
    ->whereHas('service', function($q) use ($technician) {
        $q->where('specialization_id', $technician->specialization_id);
    });
```

---

### 2. تعديل دالة `submitPricing()` في RequestController

**قبل التعديل:**
```php
// كان يحدث حالة الطلب فقط
$requestData->update(['status' => 'pricing_pending']);
```

**بعد التعديل:**
```php
// الآن يحدث حالة الطلب ويعين الفني للطلب
$requestData->update([
    'status' => 'pricing_pending',
    'assigned_technician_id' => $technician->id,
]);
```

---

### 3. تعديل دالة `pricing()` في RequestController

**قبل التعديل:**
```php
// كان يتحقق فقط من حالة الطلب
if (!in_array($request->status, ['approved', 'pricing_pending'])) {
    return back()->with('error', 'هذا الطلب غير متاح للتقديم');
}
```

**بعد التعديل:**
```php
// الآن يتحقق من حالة الطلب وأنه غير معين لفني آخر
if ($request->status !== 'approved') {
    return back()->with('error', 'هذا الطلب غير متاح للتقديم');
}

// التحقق من أن الطلب غير معين لفني آخر
if ($request->assigned_technician_id && $request->assigned_technician_id !== $technician->id) {
    return back()->with('error', 'هذا الطلب تم التقديم عليه من فني آخر');
}
```

---

## كيف يعمل النظام الآن؟

### 1. عرض الطلبات المتاحة (صفحة "طلبات متاحة")
- ✅ يعرض فقط الطلبات بحالة `approved`
- ✅ يعرض فقط الطلبات غير المعينة لأي فني (`assigned_technician_id = NULL`)
- ✅ يعرض فقط الطلبات التي تتطابق مع تخصص الفني

### 2. عند تقديم الفني على طلب
- ✅ يتم تعيين الفني للطلب (`assigned_technician_id = technician_id`)
- ✅ يتم تغيير حالة الطلب إلى `pricing_pending`
- ✅ يختفي الطلب من قائمة "طلبات متاحة" لجميع الفنيين الآخرين
- ✅ يظهر الطلب في صفحة "الطلبات المقدم عليها" للفني الذي قدم عليه فقط

### 3. عند رفض العميل للعرض
- ✅ يتم إلغاء تعيين الفني (`assigned_technician_id = NULL`)
- ✅ يتم إرجاع حالة الطلب إلى `approved`
- ✅ يظهر الطلب مرة أخرى في قائمة "طلبات متاحة" لجميع الفنيين

---

## مثال عملي

### السيناريو:
1. **العميل** يضيف طلب صيانة كهرباء
2. **الأدمن** يوافق على الطلب (حالة: `approved`)
3. الطلب يظهر لجميع الفنيين المتخصصين في الكهرباء

### عند تقديم الفني الأول:
- **الفني أحمد** يقدم عرض سعر
- الطلب يتم تعيينه للفني أحمد (`assigned_technician_id = ahmed_id`)
- حالة الطلب تتغير إلى `pricing_pending`
- الطلب يختفي من قائمة "طلبات متاحة" للفنيين الآخرين (محمد، علي، خالد)
- الطلب يظهر فقط في صفحة "الطلبات المقدم عليها" للفني أحمد

### إذا رفض العميل العرض:
- يتم إلغاء تعيين الفني أحمد
- حالة الطلب ترجع إلى `approved`
- الطلب يظهر مرة أخرى لجميع الفنيين (أحمد، محمد، علي، خالد)

---

## الفوائد

### 1. تجنب التكرار
- ✅ لا يقدم أكثر من فني على نفس الطلب في نفس الوقت
- ✅ لا يضيع وقت الفنيين في تقديم عروض لطلبات تم التقديم عليها

### 2. تحسين تجربة المستخدم
- ✅ الفني يرى فقط الطلبات المتاحة فعلاً
- ✅ العميل يتلقى عرض واحد في كل مرة ويمكنه قبوله أو رفضه

### 3. تنظيم أفضل
- ✅ كل طلب معين لفني واحد فقط
- ✅ سهولة تتبع الطلبات والعروض

---

## الصفحات المتأثرة

### 1. صفحة "طلبات متاحة" (technician.repair-requests.index)
- تعرض فقط الطلبات المتاحة للتقديم
- لا تعرض الطلبات التي قدم عليها فنيين آخرين

### 2. صفحة "الطلبات المقدم عليها" (technician.my-requests)
- تعرض الطلبات التي قدم عليها الفني الحالي فقط
- تعرض الطلبات بحالات: pricing_pending, in_progress, completed

### 3. صفحة "تقديم عرض سعر" (technician.repair-requests.pricing)
- تتحقق من أن الطلب متاح للتقديم
- تمنع التقديم إذا كان الطلب معين لفني آخر

---

## حالات الطلب

| الحالة | الوصف | يظهر في "طلبات متاحة" | يظهر في "طلباتي" |
|--------|-------|---------------------|------------------|
| `pending` | في انتظار موافقة الأدمن | ❌ | ❌ |
| `approved` | معتمد وجاهز للتقديم | ✅ (إذا لم يكن معين) | ❌ |
| `pricing_pending` | تم التقديم عليه | ❌ | ✅ (للفني المعين فقط) |
| `in_progress` | قيد التنفيذ | ❌ | ✅ (للفني المعين فقط) |
| `completed` | مكتمل | ❌ | ✅ (للفني المعين فقط) |

---

## الخلاصة

✅ **تم تطبيق التحديث بنجاح!**

الآن:
- الطلب يظهر فقط للفني الذي قدم عليه
- باقي الفنيين لا يرون الطلب بعد التقديم عليه
- إذا رفض العميل العرض، يعود الطلب للظهور لجميع الفنيين

---

**تم إعداد التقرير بواسطة:** Amazon Q Developer  
**التاريخ:** 2025  
**الحالة:** ✅ مكتمل
